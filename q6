//Use the ping command to test connectivity between two devices on a local network by specifying their IP addresses. Interpret the output to identify any packet loss or latency issues.
ping 127.0.0.1

